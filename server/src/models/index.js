const User = require("./user");

// User.hasMany(Address);
// User.hasMany(Order);
// Order.belongsTo(User);
// Order.belongsTo(Address);
// ShoppingList.belongsTo(Order);
// BillPhoto.belongsTo(Order);

module.exports = {
  User,
};
